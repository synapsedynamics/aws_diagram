#!/usr/bin/env bash
./insertCredentials_.bash
#for tfconfig in internal_lb public_albs rds_seequail elasticache_dominos ; do
#  cd "$base_dir"
#  cd ./"$tfconfig" ; terraform init ; terraform apply -auto-approve
#done
